describe('homepage', () => {
  it('should render correctly', () => {
    cy.visit('https://example.cypress.io')
  })
})


//intercept
//1 describe per file

//test all user stories

//header, button, main page has title, have jobs displayed inside of the container
//each action   can find job based on the title, can click on it, can see details
//error handling: when no data: when fetch call fails? display the error page :: 400 code, :: 500 code

//single page view:
//navigation

//favorites view :
// navigation can move in all directions thru buttons to routing pages
